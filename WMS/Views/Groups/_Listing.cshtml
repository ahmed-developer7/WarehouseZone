@model IEnumerable<Ganedata.Core.Models.GroupsViewModel>
@using Ganedata.Core.Entities.Helpers

@section scripts {

    @Html.DevExpress().GetStyleSheets(
        new StyleSheet { ExtensionType = ExtensionType.GridView, Theme = GaneStaticAppExtensions.DevexTheme }
        )

    @Html.DevExpress().GetScripts(
        new Script { ExtensionType = ExtensionType.GridView }
        )
}

@{
    ViewBag.Title = "Groups";
    ViewBag.SectionTitle = "Groups";
}

<h2><span>Groups</span></h2>
<div class="table-responsive col-md-12 p-0">
    @if (ViewBag.Message != null)
    {
        <div class="alert-success col-md-12">
            @ViewBag.Message
        </div>
    }

    @Html.Action("GroupsListPartial")
</div>

<div class="col-md-offset-2 col-md-10 bottom-right">
    <a class="btn btn-primary" href="/groups/add"><b class="fa fa-plus"></b> Add Groups</a>
</div>